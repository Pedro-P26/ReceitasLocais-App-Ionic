import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OrigembacalhauPage } from './origembacalhau.page';

describe('OrigembacalhauPage', () => {
  let component: OrigembacalhauPage;
  let fixture: ComponentFixture<OrigembacalhauPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OrigembacalhauPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
